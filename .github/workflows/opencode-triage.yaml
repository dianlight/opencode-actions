name: opencode-triage (deprecated)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual trigger"
        required: false
        default: "deprecated — no-op"

permissions:
  contents: read

jobs:
  noop:
    name: No-op (deprecated)
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║                                                       ║"
          echo "║   This workflow is deprecated.                        ║"
          echo "║                                                       ║"
          echo "║   The 'opencode-triage' action has been replaced by the      ║"
          echo "║   dedicated workflows in this repository:             ║"
          echo "║                                                       ║"
          echo "║     • opencode-triage-issue.yaml                         ║"
          echo "║     • opencode-triage-pr.yaml                            ║"
          echo "║                                                       ║"
          echo "║   Please migrate to the appropriate workflow.         ║"
          echo "║   This workflow performs no operations.               ║"
          echo "║                                                       ║"
          echo "╚══════════════════════════════════════════════════════════╝"
      - name: Exit successfully (no-op)
        run: exit 0
